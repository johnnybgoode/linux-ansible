# .github/workflows/provision.yml
name: Provision Sprite
run-name: "Provision: ${{ inputs.sprite_name }}"

on:
  workflow_dispatch:
    inputs:
      sprite_name:
        description: "Name of the sprite to provision"
        required: true
      repo_url:
        description: "GitHub repo URL to clone (leave blank to init empty repo)"
        required: false
        default: ""

jobs:
  provision:
    runs-on: ubuntu-latest
    env:
      SPRITE_TOKEN: ${{ secrets.SPRITE_TOKEN }}

    steps:
      - name: Checkout playbooks
        uses: actions/checkout@v4

      - name: Install Ansible
        run: pip install ansible

      - name: Install sprite CLI
        run: |
          curl -fsSL https://sprites.dev/install.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Run provisioning playbook
        working-directory: sprites
        env:
          SPRITE_NAME: ${{ inputs.sprite_name }}
          REPO_URL: ${{ inputs.repo_url }}
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
          # Committer identity for git on the sprite. Set these as repo-level
          # Actions variables (Settings > Secrets and variables > Actions >
          # Variables). If unset they pass through empty and the git role
          # skips identity config rather than committing a bogus identity.
          GIT_USER_NAME: ${{ vars.GIT_USER_NAME }}
          GIT_USER_EMAIL: ${{ vars.GIT_USER_EMAIL }}
        run: |
          ansible-playbook provision.yml \
            -i inventory/ -i "$SPRITE_NAME," \
            --limit "$SPRITE_NAME" \
            -e "repo_url=$REPO_URL" \
            -e "git_user_name=$GIT_USER_NAME" \
            -e "git_user_email=$GIT_USER_EMAIL" \
            --vault-password-file <(echo "$ANSIBLE_VAULT_PASSWORD")
